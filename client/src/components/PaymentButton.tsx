import React from "react";

const PaymentButton: React.FC = () => (
  <button disabled style={{ padding: '0.5rem 1rem', background: '#eee', border: '1px solid #ccc', borderRadius: 4 }}>
    Payment (Stub)
  </button>
);

export default PaymentButton;
